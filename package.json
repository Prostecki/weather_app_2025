{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Weather application with Node.js backend and React frontend",
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "install-all": "pnpm install"
  }
}